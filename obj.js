const iphone1 = {
    model: "Xs max",
    color: "Rose Gold",
    camera: {
        count: 2,
        megapixel: 32
    },
    lte: true,
    getFullName: () => { return `${model} - ${color}`} 

}

console.log(iphone1.getFullName())

for(let superman of keys){
    console.log(superman)
}

for(let i=0; i<keys.length;i++){
    const superman = keys[i]
    console.log(key)
}